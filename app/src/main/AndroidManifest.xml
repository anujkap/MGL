<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.applutions.mgl">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MGL">
        <activity android:name=".modules.KitSupplier.KitSupplierActivity"></activity>
        <activity android:name=".modules.CylinderTesting.CylinderTestingActivity" />
        <activity android:name=".modules.CarModels.CarModelActivity" />
        <activity android:name=".modules.About.AboutActivity" />
        <activity android:name=".modules.SavingsCalc.SavingsActivity" />
        <activity android:name=".modules.Home.HomeActivity" />
        <activity android:name=".modules.Auth.AccountCreatedActivity" />
        <activity android:name=".modules.Auth.SignUpDetailsActivity" />
        <activity android:name=".modules.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".modules.Auth.AuthActivity" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>